<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_interruptor"
        android:checked="false"
        android:icon="@drawable/ic_equalizer_black_24dp"
        android:title="@string/title_interruptor"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_midi"
        android:checked="false"
        android:icon="@drawable/ic_baseline_library_music_24"
        android:title="@string/midi_songs"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_midi_player"
        android:checked="false"
        android:icon="@drawable/ic_audiotrack_black_24dp"
        android:title="@string/current_song"
        app:showAsAction="ifRoom"/>

</menu>
